import { FiltrarTurnosPipe } from './filtrar-turnos.pipe';

describe('FiltrarTurnosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarTurnosPipe();
    expect(pipe).toBeTruthy();
  });
});
