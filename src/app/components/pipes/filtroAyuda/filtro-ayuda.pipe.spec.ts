import { FiltroAyudaPipe } from './filtro-ayuda.pipe';

describe('FiltroAyudaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAyudaPipe();
    expect(pipe).toBeTruthy();
  });
});
