import { AgrandarbtnDirective } from './agrandarbtn.directive';

describe('AgrandarbtnDirective', () => {
  it('should create an instance', () => {
    const directive = new AgrandarbtnDirective();
    expect(directive).toBeTruthy();
  });
});
